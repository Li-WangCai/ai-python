- ```python
  seed=random(range)#随机种子
  fitness=f(seed) #计算适应性
  survive_rate=exp(fitness)/sum(exp(fitness)) #生存概率
  parents=choose(n,seed,survive_rate) #根据生存概率选择亲本
  random_weight=random(n) #生成随机权重
  child=mix(parents，random_weight) #根据亲本加权平均计算子代
  seed=sort(child+seed,n,key=fitness)#将亲代子代合在一起，根据适应性选出n个座位新的一组
  goto 2 #迭代
  ```