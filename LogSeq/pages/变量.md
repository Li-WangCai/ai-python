- 整型，浮点数，字符串，类型函数，类型转换
	- ```python
	  a=100
	  type(a)=int
	  a="100"
	  type(a)=str
	  b=int(a)
	  type(b)=int
	  c=float(b)
	  type(c)=float
	  
	  ```
- 逻辑值 bool
	- ```python
	  x=3>2
	  type(x)
	  # x=True, type(x)=bool
	  ```